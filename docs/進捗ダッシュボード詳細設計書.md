# プロジェクト進捗管理画面 詳細設計書

## 0. 本書について

- 本書は [進捗ダッシュボード基本設計書](./進捗ダッシュボード基本設計書.md)(以下「基本設計書」)で定義した方式を、実装に着手できる粒度まで詳細化するものである。要件そのものの根拠は [進捗ダッシュボード要件定義書](./進捗ダッシュボード要件定義書.md)を参照する。
- 対象読者: 本機能を実装するエンジニア。既存Todo機能([src/app/todo](../src/app/todo)、[src/lib/actions/tasks.ts](../src/lib/actions/tasks.ts)、[src/components/todo/](../src/components/todo/))の実装パターンを前提知識として記述している箇所がある。
- 画面ごとの表示項目・操作・バリデーションメッセージ等、利用者から見た仕様は [進捗ダッシュボード機能仕様書](./進捗ダッシュボード機能仕様書.md)を正とする。本書は内部実装(型・関数・コンポーネント構成・アルゴリズム)を正とする。両者は5.x/6.xの画面IDで対応させている。

### 0.1 既存コードとの整合方針(実装時の前提)

既存コードの調査により、本機能で踏襲すべき慣習を以下のとおり確認済みである。

| 慣習 | 既存コードでの例 | 本機能での方針 |
| --- | --- | --- |
| Server Actionは`"use server"`ファイル冒頭+更新後`revalidatePath`+最新データを返す | [tasks.ts](../src/lib/actions/tasks.ts) 全関数 | 踏襲する |
| クライアント側は`try { await action(); setState(fresh); toast.success() } catch { toast.error("固定文言") }`で、サーバー側エラーの詳細メッセージはクライアントに渡さない | [todo-view.tsx:185-293](../src/components/todo/todo-view.tsx) | **一部変更**: 業務ルール由来で動的な文言が必須なもの(プロジェクト名重複、成果物種類の削除不可 等)は例外ではなく`{ ok: false, ... }`型の戻り値で表現し、呼び出し側で分岐する。システムエラー(DB接続断等)は従来どおり例外+固定文言でよい。理由は基本設計書 9章、詳細は2.5節参照 |
| バリデーションは外部ライブラリ(zod等)を使わず、プレーンなTS関数(例: `isDateRangeInvalid`)で行う | [date-range-fields.tsx:5-10](../src/components/todo/date-range-fields.tsx) | 踏襲する。zod等は新規導入しない |
| フォームは「draft state + isDirty比較」でuncontrolled送信を避ける | [task-detail-panel.tsx:49-70](../src/components/todo/task-detail-panel.tsx) | 踏襲する |
| 一覧+詳細/フォームダイアログ+ゴミ箱ダイアログの3点セット | [todo-view.tsx](../src/components/todo/todo-view.tsx) + [trash-dialog.tsx](../src/components/todo/trash-dialog.tsx) | プロジェクト管理画面はゴミ箱を「タブ」にする点のみ異なる(要件定義書 5.4) |
| ステータスの見た目は`XXX_META: Record<Status, { label, badge, dot }>` + ボタン群のPicker | [status.ts](../src/components/todo/status.ts) + [status-picker.tsx](../src/components/todo/status-picker.tsx) | 踏襲する(2.2節) |

---

## 1. データベース詳細設計

### 1.1 Prismaスキーマ(実装用・確定版)

基本設計書 5.2節のドラフトを実装確定版とする(内容は同一のため、[schema.prisma](../prisma/schema.prisma)への追記はそのまま用いる)。マイグレーションは以下の順に実施する。

```bash
# 1. schema.prisma に基本設計書 5.2節のモデルを追記
npx prisma migrate dev --name add_progress_dashboard_models
# 2. sqlite-vec拡張を使う場合のみ、生SQLで仮想テーブルを追加(1.3節)
npx prisma migrate dev --name add_document_chunk_vector_index --create-only
# → 生成された空マイグレーションファイルに1.3節のDDLを手書きし、migrate devで適用
```

- 既存の`Task`テーブルとは無関係のため、既存マイグレーション([prisma/migrations](../prisma/migrations))には影響しない。
- `prisma/seed.js`は既存Todo用データのみを投入している。本機能用の初期データ(`GlobalDeliverableType`の初期セット等)投入が必要な場合は、`prisma/seed.js`に追記するのではなく、要否を実装着手時に確認する(初期マスタが空でもUIから追加できるため必須ではない。運用上あった方が親切、程度の位置づけ)。

### 1.2 ステータス値の定数化

`Deliverable.status`(DB上は`String`)がとりうる値は以下の5つに固定する。DBレベルでは`CHECK`制約を持たない(SQLite+Prismaの標準構成に合わせ、既存`Task.status`と同様にアプリケーション層でのみ制約する)。

```typescript
// src/components/dashboard/types.ts
export type DeliverableStatus =
  | "not_started"
  | "in_progress"
  | "on_hold"
  | "in_review"
  | "done";

export const DELIVERABLE_STATUSES: DeliverableStatus[] = [
  "not_started",
  "in_progress",
  "on_hold",
  "in_review",
  "done",
];
```

### 1.3 ベクトル検索のDDL(sqlite-vec採用時)

基本設計書 7.3節の第一候補方針に基づき、`DocumentChunk.embedding`(`Bytes`)とは別に、ANN検索用の仮想テーブルを生SQLマイグレーションで追加する。

```sql
-- migration: add_document_chunk_vector_index
CREATE VIRTUAL TABLE IF NOT EXISTS document_chunk_vec USING vec0(
  chunk_id TEXT PRIMARY KEY,
  embedding FLOAT[768]  -- 採用embeddingモデルの次元数に合わせて実装時に確定
);
```

- アプリケーションからは`DocumentChunk`作成時に`document_chunk_vec`へも同じ`id`でINSERTし、削除時(`DesignDocument`のCascade削除)は`DocumentChunk`削除に追随して`document_chunk_vec`側も明示的に`DELETE`する(Prismaの`onDelete: Cascade`は仮想テーブルには効かないため、`deleteDesignDocumentInternal`相当の処理で両方削除する)。
- `sqlite-vec`拡張のロードに失敗する環境(ビルド不可等)を想定し、7章のRAG実装は「ベクトル検索アダプタ」インターフェースの背後に隠蔽し、フォールバック実装(全件コサイン類似度計算)に差し替え可能な構成とする(7.2節)。

---

## 2. 共通ロジック詳細設計

### 2.1 型定義(`src/components/dashboard/types.ts`)

[src/components/todo/types.ts](../src/components/todo/types.ts)の構成を踏襲する。

```typescript
export type DeliverableStatus =
  | "not_started" | "in_progress" | "on_hold" | "in_review" | "done";

export type CellAggregateStatus =
  | "not_applicable" | "partially_delayed" | "done" | "not_started" | "in_progress";

export type Project = {
  id: string;
  name: string;
  designDocFolderPath: string;
  isArchived: boolean;
  createdAt: string;
};

export type TrashedProject = Project & {
  deletedAt: string;
  remainingDays: number; // max(0, 30 - 経過日数)。0でも一覧には残り続ける(自動物理削除しないため)
};

export type Phase = { id: string; projectId: string; name: string; order: number };
export type Feature = { id: string; projectId: string; name: string; order: number };

export type DeliverableType = {
  id: string;
  projectId: string;
  name: string;
  defaultPhaseId: string | null;
  order: number;
  deliverableCount: number; // 削除可否判定用。一覧取得時にcountを同梱
};

export type GlobalDeliverableType = {
  id: string;
  name: string;
  defaultPhaseName: string;
  order: number;
};

export type Deliverable = {
  id: string;
  projectId: string;
  featureId: string;
  phaseId: string;
  typeId: string | null;
  name: string;
  assignee: string;
  status: DeliverableStatus;
  plannedStartDate: string | null;
  plannedEndDate: string | null;
  actualStartDate: string | null;
  actualEndDate: string | null;
  progress: number;
  memo: string;
  order: number;
};

export type DesignDocument = {
  id: string;
  projectId: string;
  title: string;
  filePath: string;
  indexStatus: "pending" | "indexed" | "failed";
  indexedAt: string | null;
  updatedAt: string;
};

export type QaSource = {
  documentId: string;
  documentTitle: string;
  sectionLabel: string;
  filePath: string;
};

export type QaHistoryItem = {
  id: string;
  question: string;
  answer: string;
  sources: QaSource[];
  createdAt: string;
};
```

- `DeliverableType.deliverableCount`は一覧取得Action側で`_count`(Prismaの`include: { _count: { select: { deliverables: true } } }`)を使って同梱し、削除ボタンの活性/非活性判定にクライアント側で即座に使えるようにする(削除試行時に毎回問い合わせる必要をなくす。ただし実際の削除実行時はサーバー側で再度カウントを取り直し、レースコンディション(削除ボタン表示後に他クライアントが成果物を追加したケース)にも対応する。詳細は3.4節)。

### 2.2 集約ステータス・遅延判定(`src/components/dashboard/aggregate-status.ts`)

基本設計書 3.2節の関数を、実装可能な完全形として以下に確定する。

```typescript
import type { Deliverable, CellAggregateStatus } from "@/components/dashboard/types";

function startOfDay(date: Date): Date {
  return new Date(date.getFullYear(), date.getMonth(), date.getDate());
}

export function isDelayed(
  d: Pick<Deliverable, "status" | "plannedEndDate">,
  now: Date = new Date()
): boolean {
  if (d.status === "done") return false;
  if (!d.plannedEndDate) return false;
  return new Date(d.plannedEndDate).getTime() < startOfDay(now).getTime();
}

export function aggregateCellStatus(
  deliverables: Pick<Deliverable, "status" | "plannedEndDate">[],
  now: Date = new Date()
): CellAggregateStatus {
  if (deliverables.length === 0) return "not_applicable";
  if (deliverables.some((d) => isDelayed(d, now))) return "partially_delayed";
  if (deliverables.every((d) => d.status === "done")) return "done";
  if (deliverables.every((d) => d.status === "not_started")) return "not_started";
  return "in_progress";
}

export function countDelayed(
  deliverables: Pick<Deliverable, "status" | "plannedEndDate">[],
  now: Date = new Date()
): number {
  return deliverables.filter((d) => isDelayed(d, now)).length;
}

export function buildMatrix(
  features: { id: string; name: string; order: number }[],
  phases: { id: string; name: string; order: number }[],
  deliverables: Deliverable[],
  now: Date = new Date()
) {
  const sortedFeatures = [...features].sort((a, b) => a.order - b.order);
  const sortedPhases = [...phases].sort((a, b) => a.order - b.order);

  return sortedFeatures.map((feature) => ({
    feature,
    cells: sortedPhases.map((phase) => {
      const cellDeliverables = deliverables.filter(
        (d) => d.featureId === feature.id && d.phaseId === phase.id
      );
      return {
        phase,
        deliverables: cellDeliverables,
        status: aggregateCellStatus(cellDeliverables, now),
        doneCount: cellDeliverables.filter((d) => d.status === "done").length,
        totalCount: cellDeliverables.length,
      };
    }),
  }));
}

export function phaseCompletionSummary(
  phase: { id: string },
  deliverables: Deliverable[]
): { done: number; total: number } {
  const inPhase = deliverables.filter((d) => d.phaseId === phase.id);
  return { done: inPhase.filter((d) => d.status === "done").length, total: inPhase.length };
}

export function projectCompletionSummary(
  deliverables: Deliverable[]
): { done: number; total: number } {
  return { done: deliverables.filter((d) => d.status === "done").length, total: deliverables.length };
}
```

- `buildMatrix`はSCR-01の描画に直接使う中核関数。`ProgressMatrixView`はサーバーから渡された生データ(`features`, `phases`, `deliverables`)をこの関数に通すだけで表全体を組み立てる。
- `now`を引数化しているのはユニットテスト容易性のため(既存コードには同種のテスト容易性配慮は明示的には無いが、遅延判定は日付境界のテストが重要なため本機能では意図的に導入する)。

### 2.3 ステータス表示メタ情報(`src/components/dashboard/status.ts`)

[src/components/todo/status.ts](../src/components/todo/status.ts)と同型。

```typescript
import type { DeliverableStatus, CellAggregateStatus } from "@/components/dashboard/types";

export const DELIVERABLE_STATUS_META: Record<
  DeliverableStatus,
  { label: string; badge: string; dot: string }
> = {
  not_started: { label: "未着手", badge: "bg-muted text-muted-foreground ring-1 ring-inset ring-border", dot: "bg-muted-foreground/50" },
  in_progress: { label: "進行中", badge: "bg-sky-500/10 text-sky-600 dark:text-sky-400 ring-1 ring-inset ring-sky-500/20", dot: "bg-sky-500" },
  on_hold:     { label: "保留",   badge: "bg-amber-500/10 text-amber-600 dark:text-amber-400 ring-1 ring-inset ring-amber-500/20", dot: "bg-amber-500" },
  in_review:   { label: "レビュー中", badge: "bg-violet-500/10 text-violet-600 dark:text-violet-400 ring-1 ring-inset ring-violet-500/20", dot: "bg-violet-500" },
  done:        { label: "完了",   badge: "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 ring-1 ring-inset ring-emerald-500/20", dot: "bg-emerald-500" },
};

export const CELL_AGGREGATE_STATUS_META: Record<
  CellAggregateStatus,
  { label: string; badge: string }
> = {
  not_applicable:    { label: "対象外", badge: "bg-muted/50 text-muted-foreground/60" },
  partially_delayed: { label: "一部遅延", badge: "bg-rose-500/10 text-rose-600 dark:text-rose-400 ring-1 ring-inset ring-rose-500/30 font-semibold" },
  done:              { label: "完了", badge: "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 ring-1 ring-inset ring-emerald-500/20" },
  not_started:       { label: "未着手", badge: "bg-muted text-muted-foreground ring-1 ring-inset ring-border" },
  in_progress:       { label: "進行中", badge: "bg-sky-500/10 text-sky-600 dark:text-sky-400 ring-1 ring-inset ring-sky-500/20" },
};
```

`DeliverableStatusPicker`コンポーネントは[status-picker.tsx](../src/components/todo/status-picker.tsx)をそのまま踏襲したボタン群として実装する(5種に増えるのみ)。

### 2.4 日付ユーティリティ

既存に日付フォーマット共通関数が無い([date-range-fields.tsx](../src/components/todo/date-range-fields.tsx)内にローカル関数`formatShortDate`があるのみ)ため、本機能ではガントビュー用に`src/components/dashboard/gantt/date-scale.ts`を新設する。

```typescript
export type GanttUnit = "week" | "month" | "quarter";

export function buildDateScale(
  unit: GanttUnit,
  rangeStart: Date,
  rangeEnd: Date
): { columns: { label: string; date: Date }[]; pxPerDay: number };

export function dateToOffsetPx(date: Date, rangeStart: Date, pxPerDay: number): number;
```

- `pxPerDay`は表示単位(週/月/四半期)ごとに固定値(例: 週=40px/日、月=12px/日、四半期=4px/日)を持つ簡易実装とし、ズーム機能は本フェーズでは持たない(要件定義書スコープ外)。

### 2.5 Server Actionのエラー表現方針

0.1節の方針に基づき、Server Actionの戻り値を2系統に分ける。

```typescript
// システムエラー(DB接続断など): 例外をthrow。クライアントはtry/catchで固定文言toastを出す(既存踏襲)

// 業務ルールエラー(利用者の操作次第で回避可能なもの): 戻り値の判別共用体で表現する
export type ActionResult<T> =
  | { ok: true; data: T }
  | { ok: false; error: ActionError };

export type ActionError =
  | { code: "DUPLICATE_PROJECT_NAME"; message: string }
  | { code: "DELIVERABLE_TYPE_IN_USE"; message: string; count: number };
```

適用対象は以下の2箇所のみ(要件定義書上、動的な件数・文言が必須なもの)。それ以外の全Actionは既存踏襲で例外+固定文言toastとする(不要な複雑化を避けるため、業務ルールエラーの型を持つのはこの2種類に限定する)。

| Action | エラー種別 |
| --- | --- |
| `createProjectAction` / `updateProjectAction` / `restoreProjectAction` | `DUPLICATE_PROJECT_NAME` |
| `deleteDeliverableTypeAction` | `DELIVERABLE_TYPE_IN_USE` |

---

## 3. Server Actions 詳細仕様

各ファイルとも冒頭に`"use server"`を置き、更新系Actionは処理完了後に`revalidatePath("/dashboard")`または`revalidatePath("/projects")`を呼ぶ([tasks.ts](../src/lib/actions/tasks.ts)踏襲)。

### 3.1 `src/lib/actions/projects.ts`

```typescript
export async function listProjectsAction(): Promise<Project[]>;
// prisma.project.findMany({ where: { deletedAt: null }, orderBy: { createdAt: "desc" } })

export async function listSwitchableProjectsAction(): Promise<Pick<Project, "id" | "name">[]>;
// where: { deletedAt: null, isArchived: false }, orderBy: { name: "asc" }

export async function createProjectAction(
  input: { name: string; designDocFolderPath: string }
): Promise<ActionResult<Project>>;
// 1. name.trim()が空なら例外(フォーム側で既に弾く想定だが念のためサーバー側でも検証)
// 2. トランザクション内で: project作成 → GlobalDeliverableType全件取得 →
//    各件をDeliverableType(projectId, name, defaultPhaseId: null, order)としてcreateMany
//    (defaultPhaseIdはコピー元にPhaseの実体が無いため常にnull。作成直後のプロジェクトにはPhaseも0件のため)
// 3. prisma.$transactionのcatchでP2002(unique制約違反)を検知した場合、
//    { ok: false, error: { code: "DUPLICATE_PROJECT_NAME", message: `プロジェクト名「${input.name}」は既に使用されています(削除済みのプロジェクトを含む)` } } を返す
// 4. revalidatePath("/projects"), revalidatePath("/dashboard")

export async function updateProjectAction(
  id: string,
  input: { name?: string; designDocFolderPath?: string }
): Promise<ActionResult<Project>>;
// createProjectActionと同様のP2002ハンドリング。name未指定時は重複チェック自体発生しない

export async function archiveProjectAction(id: string): Promise<Project>;
export async function unarchiveProjectAction(id: string): Promise<Project>;
// prisma.project.update({ where: { id }, data: { isArchived: true/false } })

export async function softDeleteProjectAction(id: string): Promise<Project[]>;
// prisma.project.update({ where: { id }, data: { deletedAt: new Date() } })
// 戻り値はlistProjectsActionの再取得結果(tasks.ts の deleteTaskAction が tree を返す方式を踏襲)

export async function listTrashedProjectsAction(): Promise<TrashedProject[]>;
// where: { deletedAt: { not: null } }, orderBy: { deletedAt: "desc" }
// remainingDays = Math.max(0, 30 - diffInDays(now, deletedAt))

export async function restoreProjectAction(
  id: string
): Promise<ActionResult<{ projects: Project[]; trashed: TrashedProject[] }>>;
// prisma.project.update({ where: { id }, data: { deletedAt: null } })
// P2002検知時: { code: "DUPLICATE_PROJECT_NAME", message: "同名の稼働中プロジェクトが存在するため復元できません" }

export async function permanentlyDeleteProjectAction(
  id: string
): Promise<{ trashed: TrashedProject[] }>;
// prisma.project.delete({ where: { id } }) → Cascadeで関連テーブル全削除(1.1節のonDelete設定による)
```

### 3.2 `src/lib/actions/phases.ts` / `features.ts`

両ファイルとも同型なので`phases.ts`を代表として記載する(`features.ts`は`phase`→`feature`に読み替え)。

```typescript
export async function createPhaseAction(
  projectId: string,
  name: string
): Promise<Phase[]>;
// order = prisma.phase.count({ where: { projectId } })
// 戻り値: プロジェクト内Phase一覧の再取得(orderAsc)

export async function updatePhaseAction(
  id: string,
  data: { name?: string; order?: number }
): Promise<Phase[]>;

export async function deletePhaseAction(id: string): Promise<Phase[]>;
// prisma.phase.delete({ where: { id } })
// onDelete: Cascadeにより配下のDeliverable・DeliverableType.defaultPhaseId(SetNull)が連動する。
// クライアント側は削除確認ダイアログで「この工程に紐づく成果物もすべて削除されます」旨を明示する(機能仕様書 5.1節)
```

### 3.3 `src/lib/actions/deliverable-types.ts`

```typescript
export async function listGlobalDeliverableTypesAction(): Promise<GlobalDeliverableType[]>;
export async function createGlobalDeliverableTypeAction(
  input: { name: string; defaultPhaseName: string }
): Promise<GlobalDeliverableType[]>;
export async function updateGlobalDeliverableTypeAction(
  id: string, data: { name?: string; defaultPhaseName?: string }
): Promise<GlobalDeliverableType[]>;
export async function deleteGlobalDeliverableTypeAction(id: string): Promise<GlobalDeliverableType[]>;
// 既存プロジェクトのDeliverableTypeとは参照関係を持たないため、使用中チェックは不要(常に削除可)

export async function listDeliverableTypesAction(projectId: string): Promise<DeliverableType[]>;
// include: { _count: { select: { deliverables: true } } } で deliverableCount を同梱(2.1節)

export async function createDeliverableTypeAction(
  projectId: string,
  input: { name: string; defaultPhaseId: string | null }
): Promise<DeliverableType[]>;

export async function updateDeliverableTypeAction(
  id: string,
  data: { name?: string; defaultPhaseId?: string | null }
): Promise<DeliverableType[]>;
// 名称・想定工程の変更は使用中でも常に可能(要件定義書 5.1節)

export async function deleteDeliverableTypeAction(
  id: string
): Promise<ActionResult<DeliverableType[]>>;
// 1. count = prisma.deliverable.count({ where: { typeId: id } })
// 2. count > 0 の場合:
//    { ok: false, error: { code: "DELIVERABLE_TYPE_IN_USE", count,
//        message: `この種類を使用している成果物が${count}件あるため削除できません` } }
// 3. count === 0 の場合のみ prisma.deliverableType.delete()
// 2.1節の deliverableCount はUI側の事前フィルタ用であり、
// 本Actionでの再カウントが正となる(TOCTOU対策として必須)
```

### 3.4 `src/lib/actions/deliverables.ts`

```typescript
export async function listDeliverablesAction(projectId: string): Promise<Deliverable[]>;
// ダッシュボード表示の主データ取得。orderBy: [{ featureId: "asc" }, { phaseId: "asc" }, { order: "asc" }]

export async function createDeliverableFromDropAction(input: {
  projectId: string;
  featureId: string;
  phaseId: string;
  typeId: string;
}): Promise<Deliverable[]>;
// 1. feature, type を取得し name初期値 `${feature.name} ${type.name}` を組み立てる
// 2. order = 対象セル(featureId+phaseId)内の件数
// 3. status: "not_started", progress: 0 で作成
// 4. 重複種類の確認ダイアログはクライアント側の責務(このAction自体は常に1件作成する。
//    「既存typeIdの有無」の事前チェックはlistDeliverablesActionで取得済みのデータをクライアントでfilterして行う)

export async function updateDeliverableAction(
  id: string,
  data: Partial<{
    name: string; assignee: string; status: DeliverableStatus;
    plannedStartDate: string | null; plannedEndDate: string | null;
    actualStartDate: string | null; actualEndDate: string | null;
    progress: number; memo: string;
  }>
): Promise<Deliverable[]>;
// status変更時の progress自動補正は既存 updateTaskAction(tasks.ts:150-171) を踏襲:
//   status === "done" → progress = 100
//   status === "not_started" → progress = 0
//   status が in_progress/on_hold/in_review になり、かつ data.progress未指定 → 現在値>0ならそのまま、0なら10に補正

export async function deleteDeliverableAction(id: string): Promise<Deliverable[]>;
// 物理削除。ゴミ箱対象外(要件定義書スコープ外・基本設計書4.1節)
```

### 3.5 `src/lib/actions/design-documents.ts`

```typescript
export async function listDesignDocumentsAction(
  projectId: string
): Promise<{ documents: DesignDocument[]; pendingCount: number; lastScannedAt: string | null }>;
// pendingCount = indexStatus !== "indexed" の件数
// lastScannedAt = documents中のupdatedAt最大値(取り込み処理を一度も実行していなければnull)

export type RescanResult = {
  added: number; updated: number; skipped: number; failed: number;
  failures: { filePath: string; reason: string }[];
};

export async function rescanDesignDocumentsAction(projectId: string): Promise<RescanResult>;
// 処理本体は 7章のingestion moduleに委譲。本Action自体はprojectのdesignDocFolderPathを取得して
// scanAndIndex(projectId, folderPath) を呼ぶだけの薄いラッパーとする
```

### 3.6 `src/lib/actions/qa.ts`

```typescript
export async function listQaHistoryAction(projectId: string): Promise<QaHistoryItem[]>;
// orderBy: { createdAt: "desc" }

export async function askQuestionAction(
  projectId: string,
  question: string
): Promise<QaHistoryItem>;
// 処理本体は 7.2節のretrieve.ts / generate-answer.tsに委譲。
// 空文字列質問はクライアント側で送信不可(送信ボタンdisabled)にするため、本Actionでは
// question.trim()が空の場合のみ例外を投げる(通常到達しない防御的チェック)
```

---

## 4. コンポーネント詳細設計

### 4.1 コンポーネントツリー

```
app/dashboard/page.tsx (Server Component)
  └─ DashboardView (Client) ── "use client"
       ├─ ProjectSwitcher (Client)
       ├─ Tabs(shadcn/ui)
       │    ├─ TabsContent "matrix" → ProgressMatrixView (Client)
       │    │     ├─ DeliverableTypePalette (Client)
       │    │     ├─ MatrixTable
       │    │     │     └─ MatrixCell × (機能数 × 工程数)
       │    │     └─ CellDetailPanel (Dialog, Client) ← セルクリックで開く
       │    │           └─ DeliverableGridItem × N
       │    ├─ TabsContent "gantt" → GanttView (Client)
       │    │     ├─ GanttFilterBar
       │    │     ├─ GanttRow(機能行) × 機能数
       │    │     │     └─ GanttRow(成果物行, indent) × 展開時のみ
       │    │     └─ GanttBar
       │    └─ TabsContent "qa" → QaView (Client)
       │          ├─ QaChatPanel
       │          ├─ QaSourceLink
       │          └─ QaHistoryList

app/projects/page.tsx (Server Component)
  └─ ProjectManagementView (Client)
       ├─ Tabs(shadcn/ui)
       │    ├─ TabsContent "list" → ProjectTable + ProjectFormDialog + GlobalDeliverableTypeSection
       │    └─ TabsContent "trash" → ProjectTrashTable(AlertDialogで完全削除確認、trash-dialog.tsxの確認UIパターン踏襲)
```

### 4.2 `DashboardView`

| 項目 | 内容 |
| --- | --- |
| props | `initialProjectId: string`, `initialTab: "matrix" \| "gantt" \| "qa"`, `switchableProjects: Pick<Project,"id"\|"name">[]` |
| state | `projects`(スイッチャー候補), 各タブの初期データはタブごとの子コンポーネントに個別props/内部stateとして委譲(1画面に全データを持たせるとタブ非表示時も再計算が走るため) |
| URL同期 | `useSearchParams`で`project`・`tab`を読み取り、`ProjectSwitcher`/`Tabs`の`onValueChange`で`router.replace(pathname + "?" + params)`する(3.1節) |
| データ取得境界 | 各タブの初期データは`app/dashboard/page.tsx`側で選択中`project`・`tab`に応じ必要な分だけサーバー取得し、非選択タブは子コンポーネントmount時にServer Actionで遅延取得する(初期表示コストを抑える。例: `tab=matrix`で開いた場合、ガント/QAのデータはタブクリック時に`useEffect`で取得) |

### 4.3 `ProgressMatrixView`

| state変数 | 型 | 用途 |
| --- | --- | --- |
| `deliverables` | `Deliverable[]` | サーバーから受領した生データ。Server Action成功後は戻り値で丸ごと置換(tasks.ts踏襲) |
| `features` / `phases` | `Feature[]` / `Phase[]` | マトリクスの行・列定義 |
| `deliverableTypes` | `DeliverableType[]` | パレット表示・警告判定用 |
| `activeCell` | `{ featureId: string; phaseId: string } \| null` | `CellDetailPanel`の開閉・対象セル |
| `dragState` | `{ typeId: string } \| null` | dnd-kitの`DragOverlay`表示用 |

**D&D実装(dnd-kit)**:
- `DndContext`を`ProgressMatrixView`のルートに配置。`sensors`は`PointerSensor`(閾値`distance: 4`でクリックと誤認しないようにする)を使用。
- `DeliverableTypePalette`内の各バッジを`useDraggable({ id: type.id, data: { type } })`。
- `MatrixCell`を`useDroppable({ id: \`${feature.id}:${phase.id}\`, data: { featureId, phaseId } })`。
- `onDragEnd`ハンドラで以下を実行:
  1. ドロップ先が無効(`over === null`)なら何もしない
  2. `type.defaultPhaseId`とドロップ先`phaseId`が不一致の場合、`window.confirm`ではなくshadcnの`AlertDialog`を開き「想定工程(基本設計)と異なる列(詳細設計)にドロップしようとしています。追加しますか?」を表示。キャンセルで中断
  3. 対象セルに既に同一`typeId`の`Deliverable`が存在する場合、別の`AlertDialog`で「同じ種類の成果物が既に存在します。追加しますか?」を表示。キャンセルで中断(2と3の両方に該当する場合は2→3の順に連続表示)
  4. 上記を通過したら`createDeliverableFromDropAction`を呼び、成功時`setDeliverables(fresh)` + `toast.success`
- キーボード操作代替: `CellDetailPanel`内に`Select`(成果物種類を選択)+「追加」ボタンを常設し、同じ`createDeliverableFromDropAction`を呼ぶ(D&Dを経由しないため、工程不一致・重複チェックはこの経路でも同様に事前確認ダイアログを挟む)。

### 4.4 `CellDetailPanel`

| 項目 | 内容 |
| --- | --- |
| 実装 | shadcnの`Dialog`(モーダル)。要件定義書は「モーダル/サイドパネル」と選択の余地を残しているが、既存Todo機能の詳細表示がサイドパネル常設型である一方、本セルは「クリックした時だけ一時的に開く」性質のためDialogを採用する |
| 表示内容 | `path`ならぬ「機能名 > 工程名」ヘッダー、`DeliverableGridItem`(カード)をグリッドで並べる。各カードに`DeliverableStatusPicker`・担当者`Input`・予定/実績日`Input[type=date]`・メモ`Textarea`を持つ(保存は個別カードごとに「保存」ボタン、またはblur時autosaveのどちらにするかは機能仕様書で確定) |
| 削除 | カードごとに削除アイコン→`AlertDialog`確認→`deleteDeliverableAction` |

### 4.5 `GanttView` / `GanttRow` / `GanttBar`

| state変数 | 型 | 用途 |
| --- | --- | --- |
| `expandedFeatureIds` | `Set<string>` | 機能行の展開状態(初期値は空集合=全折りたたみ。要件定義書 9章確定事項どおり) |
| `unit` | `GanttUnit`(`"week"\|"month"\|"quarter"`) | 表示単位切替 |
| `filter` | `{ featureId: string \| null; phaseId: string \| null }` | フィルタ(クライアント側のみ、再フェッチなし) |

- `GanttBar`は`position: absolute`で`left: dateToOffsetPx(...)`, `width: (endOffset - startOffset)`をpx指定。予定バー(背景・半透明)と実績バー(前景・塗り)を同一セル内に重ねる2層構造。
- バークリックで`Popover`(shadcn/ui、新規追加コンポーネント)を開き、予定日・実績日・遅延日数(`isDelayed`がtrueの場合のみ`(now - plannedEndDate)`日数を表示)を表示する。

### 4.6 `QaView`

| state変数 | 型 | 用途 |
| --- | --- | --- |
| `documents` | `DesignDocument[]` | 未取り込み件数バッジ表示用 |
| `rescanning` | `boolean` | 「再取り込み」ボタンの`disabled`+スピナー制御 |
| `history` | `QaHistoryItem[]` | チャット欄・履歴一覧の両方に使う単一の配列(新しいものが先頭) |
| `question` | `string` | 入力欄の controlled value |
| `asking` | `boolean` | 回答生成中のローディング表示(`useTransition`の`isPending`で代替可) |

- 「再取り込み」押下: `setRescanning(true)` → `rescanDesignDocumentsAction` → 結果サマリを`toast`(例: `toast.success(`追加${added}件・更新${updated}件・失敗${failed}件`)`) → `listDesignDocumentsAction`で`documents`再取得 → `setRescanning(false)`。
- 質問送信: `history`の先頭に楽観的に「回答生成中」のプレースホルダ行を追加してから`askQuestionAction`を呼び、成功時に置き換える(数秒〜数十秒かかるため、体感速度向上のため楽観的UI更新を行う。失敗時はプレースホルダ行を除去し`toast.error`)。

### 4.7 `ProjectManagementView` / `ProjectTrashTab`

- `Tabs`のvalueは内部state(`"list" | "trash"`)で管理。URLクエリとの同期は必須要件ではないため行わない(要件定義書はタブであることのみを求めており、ディープリンクは要求されていない)。
- ゴミ箱タブは`trash-dialog.tsx`の一覧描画パターン(空状態・ローディング表示の文言含む)を踏襲しつつ、ダイアログではなくテーブル行として表示する点のみ異なる。
- 完全削除ボタンは既存の`AlertDialog`パターン([trash-dialog.tsx](../src/components/todo/trash-dialog.tsx))をそのまま流用し、「元に戻せません」旨を明示する。

---

## 5. RAGパイプライン実装詳細

### 5.1 テキスト抽出(`src/lib/design-docs/extract-text.ts`)

| 拡張子 | ライブラリ | 抽出方式 |
| --- | --- | --- |
| `.md` | なし(標準`fs`) | そのままテキストとして読み込み、`#`〜`######`見出しを`sectionLabel`境界として使う |
| `.txt` | なし | そのまま読み込み。`sectionLabel`は無いため「ファイル先頭からのN文字目」等の代替表現を使う(詳細は機能仕様書) |
| `.docx` | `mammoth` | `mammoth.convertToHtml`→見出しタグ(`h1`〜`h3`)を`sectionLabel`境界として使う |
| `.xlsx` | `xlsx`(SheetJS) | シートごとにテキスト化し、`sectionLabel`は「シート名」とする |
| `.pdf` | `pdf-parse` | ページ単位でテキスト抽出し、`sectionLabel`は「pageN」とする(PDFは論理的な見出し構造を持たないため) |

### 5.2 チャンク分割(`src/lib/design-docs/chunk.ts`)

```typescript
export type Chunk = { sectionLabel: string; content: string; chunkIndex: number };

export function chunkText(
  sections: { label: string; text: string }[],
  options: { maxChars: number; overlapChars: number } = { maxChars: 800, overlapChars: 100 }
): Chunk[];
```

- セクション(見出し・シート・ページ)単位を最優先の分割境界とし、1セクションが`maxChars`を超える場合のみ文字数ベースでさらに分割する(`overlapChars`分を前チャンク末尾から持ち越し、文脈の連続性を保つ)。
- `maxChars`/`overlapChars`の具体値(800字/100字)は暫定値。採用embeddingモデルのコンテキスト長・応答品質検証後に調整する(基本設計書 10章の持ち越し事項)。

### 5.3 embedding生成・保存(`src/lib/design-docs/embed.ts` + `scan-folder.ts`)

```typescript
export async function scanAndIndex(projectId: string, folderPath: string): Promise<RescanResult> {
  // 1. folderPathを再帰的に走査し、対応拡張子(.md/.txt/.docx/.xlsx/.pdf)のファイル一覧を取得
  // 2. 各ファイルについて fileHash = sha256(ファイル内容) を算出
  // 3. 既存 DesignDocument(projectId, filePath)を検索
  //    - 存在せず → 新規: extractText → chunkText → 各チャンクをembed → DesignDocument+DocumentChunkを作成(indexStatus="indexed")
  //    - 存在してhash一致 → skip
  //    - 存在してhash不一致 → 既存DocumentChunkを全削除してから新規作成と同じ処理(indexStatus="indexed")
  //    - 抽出/embedding中に例外 → DesignDocumentをindexStatus="failed"で保存(またはupdate)、failuresに理由を追加して次のファイルへ
  // 4. 走査で見つからなくなった既存DesignDocument(フォルダから削除されたファイル)は本処理では削除しない
  //    (「取り込み」は追加/更新検出が要件であり、ファイル削除の検知・追随は要件定義書に明記が無いため対象外とする。
  //     detected as a follow-up topic if requested)
  // 5. 集計結果(added/updated/skipped/failed)を返す
}
```

- Ollama embeddings APIは`src/lib/llm/ollama-client.ts`の`embed(texts: string[]): Promise<number[][]>`経由で呼ぶ。1リクエストで複数チャンクをまとめて送るか1件ずつ送るかはOllama側API仕様に依存するため実装時に確定。
- 5.4節フォールバック時は`DocumentChunk.embedding`(`Bytes`)に`Float32Array`をシリアライズして保存する。`sqlite-vec`採用時は同内容を`document_chunk_vec`仮想テーブルにも複製する(1.3節)。

### 5.4 類似検索・回答生成(`src/lib/qa/retrieve.ts` + `generate-answer.ts`)

```typescript
// retrieve.ts
export async function retrieveRelevantChunks(
  projectId: string, questionEmbedding: number[], topK: number = 5
): Promise<(DocumentChunk & { document: DesignDocument })[]>;
// sqlite-vec採用時: document_chunk_vec に対する `SELECT ... ORDER BY distance LIMIT topK` の生SQL
// フォールバック時: projectId配下の全DocumentChunkをロードし、コサイン類似度をJS側で計算してtopK件をソート抽出
//   (非機能要件の想定規模(設計書に付随するチャンク数は概ね数百〜数千オーダー)であれば全件計算でも
//    GPUなし/RAM8GB上で許容範囲と想定。実測結果次第でsqlite-vec必須化を再検討する)

// generate-answer.ts
export async function generateAnswer(
  question: string, chunks: (DocumentChunk & { document: DesignDocument })[]
): Promise<{ answer: string; sources: QaSource[] }>;
// プロンプトテンプレート(暫定):
//   system: "あなたはプロジェクトの設計書に基づいて質問に答えるアシスタントです。
//            提供された参照情報のみを根拠に回答し、根拠が無い場合は「設計書からは判断できません」と答えてください。"
//   user: "# 参照情報\n" + chunks.map(c => `## ${c.document.title} - ${c.sectionLabel}\n${c.content}`).join("\n\n")
//          + "\n\n# 質問\n" + question
// sources = chunksから (documentId, documentTitle, sectionLabel, filePath) の重複排除リストを生成
```

---

## 6. 依存パッケージ追加一覧

[package.json](../package.json)への追加を以下のとおり計画する(実装着手時に`npm install`)。

| パッケージ | 用途 | 種別 |
| --- | --- | --- |
| `@dnd-kit/core` | 全体進捗ビューのドラッグ&ドロップ | dependencies |
| `mammoth` | .docx抽出 | dependencies |
| `xlsx` | .xlsx抽出 | dependencies |
| `pdf-parse` | .pdf抽出 | dependencies |
| `sqlite-vec`(npm経由の拡張ロードヘルパー、または`better-sqlite3`+拡張ファイル直接ロード) | ベクトル検索(5.4節で採用確定した場合のみ) | dependencies |

追加のshadcn/uiコンポーネント(`npx shadcn add <name>`で生成。[components.json](../components.json)の設定に従う):

| コンポーネント | 用途 |
| --- | --- |
| `select` | プロジェクト切替、成果物種類プルダウン選択(D&D代替) |
| `tabs` | ダッシュボード3タブ、プロジェクト管理2タブ |
| `table` | プロジェクト一覧、ゴミ箱一覧 |
| `progress` | 完了率のプログレスバー表示(任意。バッジのみで済ませる場合は不要) |
| `popover` | ガントバーの詳細表示 |
| `scroll-area` | マトリクス表・ガントチャートの横スクロール領域 |

---

## 7. 環境変数・設定

| 変数名 | 用途 | 既定値(開発時) |
| --- | --- | --- |
| `OLLAMA_HOST` | Ollama REST APIのベースURL | `http://localhost:11434` |
| `OLLAMA_CHAT_MODEL` | QA回答生成に使うモデル名 | 実装フェーズで確定(基本設計書 7.3節) |
| `OLLAMA_EMBED_MODEL` | embedding生成に使うモデル名 | 実装フェーズで確定 |

`.env`([.env](../.env))に追記し、既存の`DATABASE_URL`と同様にNext.jsのサーバーサイド専用値として扱う(クライアントに公開しないため`NEXT_PUBLIC_`prefixは付けない)。

---

## 8. 詳細設計時点での残課題

- 5.3節のembedding次元数(`FLOAT[768]`)は暫定値。採用モデル確定後にDDLを更新する。
- 5.4節のフォールバック実装(全件コサイン類似度)の性能実測、`sqlite-vec`必須化判断。
- `CellDetailPanel`内カードの保存トリガー(個別保存ボタン方式 or blur時autosave方式)は機能仕様書側で確定する。
- ガントビューの日付スケール(`pxPerDay`)の具体値はUIレビューで調整の余地あり。
